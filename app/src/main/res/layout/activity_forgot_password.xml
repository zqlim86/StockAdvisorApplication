<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/backgroundwhite"
    tools:context=".ForgotPasswordActivity">

    <!-- Id of this TextView is linkLogin -->
    <!-- The textColor of this is @color/lightpurple -->
    <!-- The textStyle of this is italic -->
    <!-- It is properly constraint vertically and horizontally, so that it doesn't go to the wrong position during runtime. -->
    <TextView
        android:id="@+id/linkLogin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="83dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="83dp"
        android:text="Remember your password? Login Here."
        android:textColor="@color/realblack"
        android:textSize="12sp"
        android:textStyle="italic"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/forgotPasswordButton" />

    <!-- Id of this TextView is forgotPasswordView -->
    <!-- The textColor of this is @color/backgroundwhite -->
    <!-- The textAlignment of this is center -->
    <!-- It is properly constraint vertically and horizontally, so that it doesn't go to the wrong position during runtime. -->
    <TextView
        android:id="@+id/forgotPasswordView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="92dp"
        android:layout_marginEnd="93dp"
        android:fontFamily="@font/gilroybold"
        android:text="Reset Password"
        android:textAlignment="center"
        android:textColor="@color/realblack"
        android:textSize="30sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.48"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.303" />

    <!-- Id of this TextView is textView17 -->
    <!-- The textColor of this is @color/backgroundwhite -->
    <!-- The fontFamily of this is @font/montserratmedium -->
    <!-- It is properly constraint vertically and horizontally, so that it doesn't go to the wrong position during runtime. -->
    <TextView
        android:id="@+id/textView17"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="36dp"
        android:layout_marginEnd="36dp"
        android:fontFamily="@font/gilroymedium"
        android:text="We will e-mail you a link to reset your password."
        android:textColor="@color/realblack"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/forgotPasswordView" />

    <!-- Id of this TextView is forgotPasswordEmail -->
    <!-- The textColor of this is @color/lightpurple -->
    <!-- The textColorHint of this is also @color/lightpurple -->
    <!-- There is drawable on the left side, using the @drawable/ic_email2 -->
    <!-- It is properly constraint vertically and horizontally, so that it doesn't go to the wrong position during runtime. -->
    <EditText
        android:id="@+id/forgotPasswordEmail"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="32dp"
        android:layout_marginTop="98dp"
        android:layout_marginEnd="32dp"
        android:background="@drawable/background_input"
        android:drawableLeft="@drawable/ic_email2"
        android:drawablePadding="7dp"
        android:ems="10"
        android:textColorHint="@color/inputhint"
        android:textColor="@color/realblack"
        android:hint="Enter Email Address"
        android:inputType="textPersonName"
        android:paddingLeft="20dp"
        android:paddingTop="10dp"
        android:paddingRight="10dp"
        android:paddingBottom="10dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/forgotPasswordView" />

    <!-- Id of this TextView is forgotPasswordButton -->
    <!-- The textColor of this is @color/white -->
    <!-- There is letterSpacing of 0.1 -->
    <!-- There is background for this widget, using the @drawable/background_button -->
    <!-- It is properly constraint vertically and horizontally, so that it doesn't go to the wrong position during runtime. -->
    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/forgotPasswordButton"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="32dp"
        android:layout_marginTop="10dp"
        android:layout_marginEnd="32dp"
        android:background="@drawable/background_button"
        android:fontFamily="@font/montserratbold"
        android:letterSpacing="0.1"
        android:text="Reset Password"
        android:textColor="@color/white"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/forgotPasswordEmail" />

    <!-- Id of this TextView is forgotPasswordProgressBar -->
    <!-- It's visibility is set to invisible -->
    <!-- It is properly constraint vertically and horizontally, so that it doesn't go to the wrong position during runtime. -->
    <ProgressBar
        android:id="@+id/forgotPasswordProgressBar"
        style="?android:attr/progressBarStyle"
        android:layout_width="96dp"
        android:layout_height="82dp"
        android:visibility="invisible"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />


</androidx.constraintlayout.widget.ConstraintLayout>